export type GalleryDto = {
  id: string
  nama: string
  deskripsi: string
  gambar: string
  slug: string
  order: number
  is_head: boolean
  created_at: string
  updated_at: string
}

export type GalleryItem = {
  id: string
  nama: string
  deskripsi: string
  gambar: string
  slug: string
  isHead: boolean
  order: number
  createdAt: string
  updatedAt: string
}
